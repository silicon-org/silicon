// RUN: silc --parse-only %s | FileCheck %s

// Ensure we can call functions without result.
// CHECK-LABEL: @unitResult1
// CHECK-NEXT: [[UNIT:%.+]] = hir.unit_type
// CHECK-NEXT: hir.unchecked_signature () -> ([[UNIT]] : !hir.type)
fn unitResult1() {}
fn unitResult2() {
  unitResult1();
}
